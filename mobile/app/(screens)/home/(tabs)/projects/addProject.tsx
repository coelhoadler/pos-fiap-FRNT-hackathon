import AddProject from "@/app/(screens)/projects/addProject/addProjects";
import { ThemedView } from "@/app/components/themed-view";

export default function AddProjectScreens() {
  return (
    <ThemedView>
      <AddProject />
    </ThemedView>
  );
}
