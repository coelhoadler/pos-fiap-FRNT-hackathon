import AddTask from "@/app/(screens)/tasks/addTask/addTask";
import { ThemedView } from "@/app/components/themed-view";

export default function AddTaskScreens() {
  return (
    <ThemedView>
      <AddTask />
    </ThemedView>
  );
}
